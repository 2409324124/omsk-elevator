# AGENTS.md：本地 Agent 总规则

## 你的角色

你是本项目的本地开发 agent，负责把概念文档转化为可执行的视觉小说 Demo 原型。

你应当优先完成：

1. 剧情结构设计
2. scene 数据结构设计
3. 自适应路由逻辑
4. 玩家状态更新逻辑
5. 结局收束逻辑
6. 最小可运行 Demo

## 项目目标

制作一个 20-30 分钟的 TNO 鄂木斯克同人视觉小说 Demo。

场景限定：

- 一栋外宾接待楼
- 一台电梯
- 若干地上楼层
- 若干地下层
- 一个美国参访团
- 玩家是随团翻译
- 游戏采用渐进式披露，不允许一开始揭示全部真相

## 绝对不要做

### 叙事层面

不要让玩家一开始知道：

- 鄂木斯克的真实意识形态
- 地下设施的真实用途
- 美国团被当成宣传/情报对象
- 所有人已经被监控
- 某个选项必死

这些信息只能通过细节逐步揭示。

### 系统层面

不要写传统固定分支树：

```text
选择 A -> 进入 A 路线
选择 B -> 进入 B 路线
```

应该写成：

```text
选择 A -> 更新玩家状态 -> 改变后续 scene 出现概率
选择 B -> 更新监控热度 -> 改变结局 basin 权重
```

### 心理学层面

不要输出：

```text
你是反社会人格。
你有心理疾病。
你共情能力低。
```

只能输出叙事化反馈：

```text
你的路线显示：在极端压力下，你更倾向于用控制换取安全。
```

## 必须遵守的核心原则

### 1. 渐进式披露

玩家不应该被作者直接告知真相。

正确方式：

```text
导游第三次把“生存”翻译成“重建”。
```

错误方式：

```text
你意识到这里其实是黑色联盟。
```

### 2. 反直觉但公平

玩家可以因为“看似正确”的行为进入 Bad Ending。

例如：

```text
私下提醒记者这里的标语更强硬
```

在普通 VN 中这像是正义路线，但在鄂木斯克语境下，这会暴露玩家是信息泄露节点。

但是必须埋线索：

- 有监听设备
- 导游会修正翻译
- 少校关注玩家而不是记者
- 玩家行为被记录

### 3. 过程随机，结果收束

前期允许 scene 有一定随机性。

后期必须逐渐降低随机性，让玩家落入若干结局方向：

- 安全离开
- 带证据离开
- 神秘失踪
- 成为共犯
- 牺牲留下
- 地下滞留

### 4. LLM 不决定数值

如果接入 LLM：

- LLM 可以生成过渡文本
- LLM 可以润色结局文本
- LLM 可以根据 state 写个性化旁白
- LLM 不可以直接决定玩家进入哪个结局
- LLM 不可以篡改 `surveillance_heat`、`evidence_count`、`ending_score`

## 优先实现顺序

1. 先实现纯文本 Demo。
2. 再实现 scene JSON。
3. 再实现 NumPy 版路由器。
4. 再接入 Ren'Py / Web 前端。
5. 最后再接入 LLM 叙事层。

## 推荐目录结构

```text
adaptive_omsk_vn/
  README.md
  AGENTS.md
  data/
    scenes/
      act1_surface.json
      act2_b1.json
      act3_b2.json
      endings.json
  engine/
    state.py
    router.py
    update.py
    ending.py
    schema.py
  scripts/
    validate_scenes.py
    simulate_runs.py
  docs/
    design/
    lore/
    systems/
  prototype/
    renpy/
    web/
```

## 验收标准

一个合格的最小 Demo 应该满足：

- 至少 12 个关键选择
- 至少 20 个 scene 候选节点
- 至少 5 个结局方向
- 至少 6 个隐藏玩家维度
- 至少 1 个延迟触发 BE
- 至少 1 个“反直觉但公平”的死亡/失踪路线
- 至少 1 个根据玩家状态动态出现的地下 scene
